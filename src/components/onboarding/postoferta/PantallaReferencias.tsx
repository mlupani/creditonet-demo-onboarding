"use client";

import { PersonasVinculadas } from "./PersonasVinculadas";

// Pantalla 4 · Referencias personales (Onboarding §7).
export function PantallaReferencias() {
  return <PersonasVinculadas tipo="referencia" />;
}
